{
  "name": "protractorproject",
  "version": "1.0.0",
  "description": "Rewaa tech project",
  "main": "conf.js",
  "scripts": {
    "pretest": "rm -rf allure-results",
    "test": "protractor /Users/stockx/unifyMySetup/protractorSetup/config/conf.js",
    "posttest": "allure generate allure-results --clean -o allure-report || true",
    "wdm.start":"webdriver-manager start"
  },
  "author": "sai",
  "license": "UNLICENSED",
  "dependencies": {
    "jasmine-allure-reporter": "^1.0.2"
  }
}
